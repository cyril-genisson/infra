name: CI
on:
  push:
    branches: [ "main" ]

jobs:
  build-and-test:
    runs-on: self-hosted
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Make executable script
        run: chmod +x app/check_app.sh
      - name: Start tests
        run: ./app/check_app.sh
